import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.makeagif.com'], // Add the domain here
  },
  /* config options here */
};

export default nextConfig;
